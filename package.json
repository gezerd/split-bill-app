{
  "name": "split-bill-app",
  "private": true,
  "scripts": {
    "setup": "node -e \"const fs=require('fs');fs.existsSync('.env')||fs.copyFileSync('.env.example','.env')\" && npm install && npm install --prefix frontend && uv sync --directory backend && npm exec --prefix frontend -- playwright install chromium",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:mock": "cross-env MOCK_OCR=true npm run dev",
    "dev:backend": "uv run --directory backend uvicorn app.main:app --reload --env-file ../.env",
    "dev:frontend": "npm run dev --prefix frontend",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "uv run --directory backend pytest",
    "test:frontend": "npm test --prefix frontend",
    "test:e2e": "npm run test:e2e --prefix frontend",
    "test:all": "npm test && npm run test:e2e"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}
